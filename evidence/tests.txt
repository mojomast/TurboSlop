
> turboslop@0.1.0 test
> tsx test/forge.test.ts && tsx test/images.test.ts && tsx test/zip.test.ts && tsx test/motifs.test.ts && tsx test/assets.test.ts && tsx test/fonts.test.ts && tsx test/session.test.ts && tsx test/slots.test.ts && tsx test/diversity.test.ts && tsx test/locks.test.ts && tsx test/revision.test.ts && tsx test/assetplan.test.ts && tsx test/viewport.test.ts


=== TurboSlop ===

  ok    local decider runs offline and returns Jev-shaped answers
  ok    every composed pick exists in the catalog (no schema drift)
  ok    spec validates against the DesignSpec schema
  ok    composite score is a weighted mean of axis confidences
  ok    low confidence is flagged for review rather than hidden
  ok    ranking is derived from the probability distribution
  ok    compose rejects a catalog/criteria drift
  ok    no generated page contains the old hardcoded studio
  ok    the specimen is derived from the brief, not a fixed brand
  ok    the specimen presents the decisions it was built from
  ok    fallback content validates against the Content schema
  ok    emphasis convention converts to <em> and strips safely
  ok    the tagline accent renders as an <em>, not literal asterisks
  ok    content drives every part of the rendered page
  ok    contact details are never invented to fill the section
  ok    a form is only rendered when it has a real submission path
  ok    every rendered id is unique, even when a blueprint repeats a module
  ok    nav anchors resolve to the instance the blueprint means
  ok    nav anchors always resolve to real section ids
  ok    every decided axis reaches the spec tokens
  ok    spec carries writer metadata even without an LLM
  ok    renderer emits a complete, self-contained document
  ok    renderer uses the modern CSS feature set
  ok    every emotion yields a non-empty atmosphere layer
  ok    reduced-motion is honoured
  ok    every blueprint renders and all its nav anchors resolve
  ok    blueprints produce distinct section sequences and block variants
  ok    two blueprints render visibly different block sequences
  ok    every effect kit emits its own scoped CSS
  ok    the same brief with a different blueprint produces a different page
  ok    over-long generated fields are clamped, not rejected
  ok    repair returns null only when the content is genuinely unusable
  ok    unbalanced emphasis asterisks are repaired, not printed
  ok    truncated JSON is salvaged instead of discarding the generation
  ok    salvage returns valid JSON untouched and declines the hopeless
  ok    extractJson falls back to salvage when parsing fails
  ok    reasoning controls use the documented field names
  ok    thinking can be turned on, and temperature is only sent when it applies
  ok    reasoning tokens are reported, and are zero when thinking is off
  ok    every design record carries per-generation metrics
  ok    cost is recomputed for specs that predate the field
  ok    every local-decider affinity id exists in the catalog
  ok    the palette guardrail reads lightness from the palette, not a name list
  ok    every blueprint validates against its own compatibility rules
  ok    blueprints are structurally distinct from one another
  ok    the blueprint catalog covers every lead
  ok    no blueprint always demands the same sections in the same order
  ok    structural distance ignores copy, colour and class names
  ok    every visual blueprint in the whole space validates
  ok    the visual blueprint is deterministic and genuinely seeded
  ok    typographic recipes are recipes, not just font names
  ok    the three new hero recipes render as genuinely different markup
  ok    a product demonstration is wrapped in a real frame
  ok    art-directed treatments degrade rather than hide text
  ok    icons are used sparingly and never as the only carrier of meaning
        -> deepseek-flash wrote "Moinho" — We grind the grain in the morning and bake it before noon.
  ok    live writing (deepseek/deepseek-flash (key from DEEPSEEK_API_KEY))
        -> emotion=mystery(1.00) composition=gallery-first(1.00) effects=cinematic-depth(0.57) palette=void-violet(0.99) typography=editorial-serif(0.82) layout=editorial-asymmetric(0.53) motion=glacial(0.94) density=balanced(0.00)
  ok    live Jev decision
        -> design by jev-1.13.0, content "Roundhouse" by deepseek-flash, 8995ms total
  ok    both halves compose: Jev designs, the writer writes

all 58 checks passed


=== turboslop / images ===

  ok    validateGenerate enforces the documented bounds
  ok    prompt is trimmed before the 1000-character check
  ok    images are OFF unless a base URL is configured
  ok    localhost is always allowed; other hosts must be allowlisted
  ok    any host works when the allowlist is explicitly disabled
  ok    non-HTTP schemes and malformed URLs are refused
  ok    optional auth header is built, bearer-prefixed only for Authorization
  ok    Origin defaults to the base URL and can be overridden
  ok    image paths are constrained to /images/*.png on the service origin
  ok    pollJobs returns ONLY the ids we submitted
  ok    submitGenerate parses a 202 and surfaces busy as retryable
  ok    the POST carries the Origin header the service requires
  ok    fetchImage rejects non-PNG payloads and oversized responses
  ok    waitForJobs returns once every job is terminal
  ok    a failed job terminates polling without cancelling service-wide work
  ok    buildAssetPrompts returns the requested count within API limits
  ok    prompts stay inside the budget for every emotion
  ok    the budget warning fires past the measured encoder limit
  ok    asset prompts describe the palette in visible terms, never by id
  ok    asset prompts avoid the anti-patterns this model ignores
  ok    presets resolve correctly and overrides win
  ok    guidance 1 is labelled as a non-equivalent shortcut
  ok    pages render identically in structure with or without generated art
  ok    a SUPPLIED image in a texture hero slot renders as the hero atmosphere
  ok    generateAssets submits, polls, downloads and persists
  ok    a flat frame is rejected, not rendered, and the slot keeps its plate
  ok    generateAssets discards a flat frame and persists nothing for it

all 27 checks passed


=== turboslop / zip ===

  info  unzip: /home/mojo/.local/bin/unzip

  ok    crc32 matches the standard IEEE test vectors
  ok    archive carries local, central and EOCD signatures with the right count
  ok    node reader round-trips nested, unicode, zero-byte and text entries
  ok    unzip -t reports integrity and entries match byte-for-byte
  ok    unicode filename survives a real unzip round trip
  ok    download filenames are header-safe: ASCII passthrough, RFC 6266 for the rest
  ok    empty archive is valid and extractable
  ok    STORE is chosen for incompressible data and DEFLATE for compressible
  ok    path traversal and absolute paths are rejected
  ok    duplicate paths are rejected with a clear error
  ok    a ~5 MB incompressible payload round-trips byte-identically

all 11 checks passed


=== TurboSlop / motifs ===

  ok    every family emits a standalone, decorative <svg>
  ok    no family emits NaN, undefined or Infinity
  ok    every svg has balanced angle brackets
  ok    geometry is recolourable and keeps a concrete ink fallback
  ok    all numeric coordinates are rounded to two decimals
  ok    same spec twice is byte-identical, for every family
  ok    different seeds produce different strings
  ok    different seeds produce different geometry, not just headers
  ok    families are visually distinct, not one drawing renamed
  ok    density 1 draws more elements than density 0
  ok    element budget: under 400 tags at 1200x800 density 1
  ok    id and filename are stable and derived from the spec
  ok    MOTIF_FAMILIES declares the twelve distinct families
  ok    every family is reachable from the catalog emotions or the fallback
  ok    motifFamilyForEmotion is total over the catalog
  ok    an unknown emotion still returns a valid family
  ok    motifToDataUri is URL-encoded, not base64, and escapes #
  ok    the data uri decodes back to the exact svg
  ok    an unknown family throws a clear error
  ok    a non-finite seed is treated as 0 rather than throwing
  ok    width and height are clamped to 64..4000
  ok    density is optional and defaults cleanly
  ok    injected colour values are sanitised before they reach markup
  ok    motifInk derives a legible ink from real palette colours

all 24 checks passed


=== TurboSlop assets ===

  ok    every frame kind renders a non-empty html string
  ok    every kind contains the exact inner string exactly once
  ok    inner is never escaped or transformed
  ok    every frame starts with a sensible figure or div tag
  ok    no frame emits undefined or NaN for any seed
  ok    a hostile label is escaped, not injected
  ok    frames are deterministic for a fixed seed
  ok    every kind applies a usable default ratio
  ok    an explicit ratio overrides the default
  ok    an unknown frame kind throws a clear error
  ok    each kind declares a unique role and marks its kind in the markup
  ok    frame colour comes from palette custom properties with fallbacks
  ok    frameForLead is total for every lead and emotion
  ok    frameForLead still returns a valid kind for unknown input
  ok    frameInset is a real per-kind number in 0..1 with plain the maximum
  ok    frameInset values are distinct per kind
  ok    every icon name renders a standalone svg
  ok    no icon emits undefined or NaN, with or without options
  ok    hasIcon recognises every declared name and rejects others
  ok    a title makes an icon labelled and not hidden
  ok    an icon without a title is hidden from assistive tech
  ok    the icon title is escaped
  ok    every icon shares the same stroke width (family guarantee)
  ok    every icon shares round caps and joins
  ok    every viewBox is the same 24 by 24 grid
  ok    every icon uses currentColor, no fill, and is not focusable
  ok    icon names are unique and numerous
  ok    every icon stays under the byte ceiling
  ok    size adds width/height; leaving it out keeps the icon scalable
  ok    an unknown icon name throws
  ok    the icon licence records original MIT work
  ok    both families parse every declared glyph
  ok    both families declare and render the same 24-grid outline profile
  ok    no glyph in any family carries scripts, events or external references
  ok    the Lucide licence document names the family, the file and the ISC notice
  ok    every icon role has curated candidates that resolve per family
  ok    every iconsFor result is deterministic per seed and resolves in its family
  ok    both families are reachable and a page never mixes them
  ok    a renamed or unknown glyph is rejected by validation

all 39 checks passed


=== TurboSlop / fonts ===

  ok    every bundled file referenced by FONT_PACK exists on disk
  ok    every bundled file is a real woff2 over 5 KB
  ok    each family ships exactly one distinct file, and all files are covered
  ok    the pack covers every direction exactly once and has no NaN/undefined
  ok    LICENSES.md exists, is non-trivial, and carries the OFL text
  ok    LICENSES.md names every bundled family and its file
  ok    LICENSES.md keeps a copyright line and the OFL notice for each family
  ok    every family licence is a real SPDX identifier whose text resolves
  ok    fontDirectionFor is total over all catalog typeface ids
  ok    fontDirectionFor maps the known ids and defaults unknown ids to restrained
  ok    fontFaceCss emits a complete @font-face for every direction
  ok    at least one bundled family exposes a real width axis
  ok    fontFaceCss honours a custom basePath
  ok    fontFaceCss returns an empty string for an unknown direction
  ok    fontFaceCssFor emits only the requested directions
  ok    fontFaceCssFor dedupes repeated directions and tolerates empty input
  ok    fontFilesFor returns only existing files with correct byte counts
  ok    fontFilesFor([]) is empty and ignores unknown directions
  ok    the whole pack stays under 1.5 MB
  ok    describeFontPack names every family without NaN or undefined

all 20 checks passed


=== TurboSlop — direction sessions ===

  ok    a session offers between 4 and 8 directions
  ok    the decision response is persisted in full
  ok    directions are structurally distinct, not recolours
  ok    every direction has a preview file on disk
  ok    previews carry no remote font request
  ok    previews are labelled as previews, with the reason
  ok    every rendered id in every preview is unique
  ok    every anchor in every preview resolves, and nav targets match the structure
  ok    exactly one #top exists, and it is the hero
  ok    previewing cost exactly one model call (no writer configured)
  ok    the shared inventory is stored once, and is honest about its origin
  ok    selecting a direction performs no model call and changes no decision
  ok    loading a session never needs a decision call
  ok    regeneration is deterministic for a given seed and costs nothing
  ok    regeneration with a different seed gives different directions
  ok    locks pin exactly the axes they name, and nothing else
  ok    locks are recorded on the session for the next regeneration
  ok    finalizing writes a real design with bundled fonts and no remote request
  ok    a finalized page never invents contact details
  ok    the bundled font files exist beside the design
  ok    sessions are listed newest-first and survive a round trip
  ok    a session id that is not well formed is refused, not joined

all 22 checks passed


=== TurboSlop — image slots ===

  ok    every blueprint that wants images declares real, unique slots
  ok    a full-bleed hero slot is declared a texture, never a photograph
  ok    slot prompts describe the place, not a count
  ok    intrinsic size is read from the file, not assumed
  ok    a supplied image is copied in, with its size, credit and licence
  ok    a supplied image with no attribution is recorded honestly, not guessed
  ok    path traversal is refused
  ok    a non-image is refused with a note rather than failing the job
  ok    an image for a slot this direction does not render is refused
  ok    an asset lands in the slot it was made for, not wherever it fits
  ok    a texture slot is marked as atmosphere in the markup
  ok    a supplied image beats a generated one for the same slot
  ok    no asset is ever referenced from a path outside the output

all 13 checks passed


=== TurboSlop — enforced layout diversity ===

  ok    festival: a six-direction set at explore=1 meets the composition/construction/treatment/grayscale targets
  ok    shop: a six-direction set at explore=1 meets the composition/construction/treatment/grayscale targets
  ok    software: a six-direction set at explore=1 meets the composition/construction/treatment/grayscale targets
  ok    editorial: a six-direction set at explore=1 meets the composition/construction/treatment/grayscale targets
  ok    service: a six-direction set at explore=1 meets the composition/construction/treatment/grayscale targets
  ok    artist: a six-direction set at explore=1 meets the composition/construction/treatment/grayscale targets
  ok    technical: a six-direction set at explore=1 meets the composition/construction/treatment/grayscale targets
  ok    at explore=0 the targets drop to the fit-first floor, at explore=1 they ask for the full spread
  ok    the candidate search is bounded, de-duplicated, and near-duplicates are rejected explicitly
  ok    no two directions in a set share a fingerprint key, and every pair is separated
  ok    the same inputs, seed and history snapshot reproduce the same result byte for byte
  ok    a different history snapshot is the input that changes the selection — still deterministically
  ok    grayscale distance drops the hue-only terms and still measures the rest
  ok    resolved fingerprints persist across sessions, scoped by project
  ok    an invalid project id is refused rather than silently writing a weird file
  ok    a single-design run records its resolved design and avoids repeating it next time
  ok    an image-enabled selection stays on layouts that can hold an image (with an honest fallback)
  ok    a fresh single-design run skips the lead the project just used
  ok    the diversity report counts what a set actually contains
  ok    the selection version is recorded and stable
  info  festival seed 11: statement-letter/arctic-cyan/stacked/plain | event-festival/hazard-mono/outline/shaped | data-metrics/hazard-mono/caps/plain | product-demo~cc6415a14/hazard-mono/stacked/cutout | story-editorial~cc67caeb4/arctic-cyan/stacked/textwrap | statement-display/arctic-cyan/stacked/plain
  info  festival seed 42: statement-letter/arctic-cyan/stacked/plain | event-festival~f52200364/arctic-cyan/outline/shaped | data-calculator/arctic-cyan/caps/plain | product-demo~f51bd2714/hazard-mono/stacked/cutout | story-origin~f520c4294/arctic-cyan/stacked/textwrap | statement-display~f519f76b4/arctic-cyan/stacked/plain
  info  festival seed 97: statement-panel/hazard-mono/stacked/plain | event-festival/hazard-mono/outline/shaped | product-spec/hazard-mono/caps/plain | catalogue-gallery~f308d1bc4/arctic-cyan/stacked/textwrap | product-demo/arctic-cyan/stacked/cutout | statement-display/arctic-cyan/stacked/plain
  info  shop seed 11: statement-letter/arctic-cyan/stacked/plain | image-mosaic~cc6b80964/hazard-mono/outline/shaped | product-demo/hazard-mono/run-on/cutout | data-calculator~cc6d5b3b4/arctic-cyan/caps/plain | story-editorial~cc67caeb4/hazard-mono/stacked/textwrap | statement-display/arctic-cyan/broken/plain
  info  shop seed 42: statement-letter/arctic-cyan/stacked/plain | event-festival~f52200364/arctic-cyan/outline/shaped | product-demo/arctic-cyan/run-on/cutout | data-calculator~f52517aa4/hazard-mono/caps/plain | catalogue-rail~f51e4aec4/arctic-cyan/broken/textwrap | story-origin/arctic-cyan/stacked/textwrap
  info  shop seed 97: statement-panel/hazard-mono/stacked/plain | image-strip~f30f00434/arctic-cyan/outline/shaped | catalogue-rail~f3096ff34/hazard-mono/run-on/textwrap | product-spec~f30795af4/arctic-cyan/caps/plain | product-demo/hazard-mono/stacked/cutout | event-ticketing~f30dc3744/hazard-mono/outline/plain
  info  software seed 11: statement-panel~cc62d9334/noir-lime/stacked/plain | image-mosaic~cc6b80964/noir-lime/outline/shaped | product-demo~cc6415a14/noir-lime/stacked/cutout | data-metrics~cc6cbd044/noir-lime/caps/plain | story-editorial~cc67cb4c4/noir-lime/stacked/textwrap | statement-display/arctic-cyan/stacked/plain
  info  software seed 42: statement-panel~f51a96034/noir-lime/stacked/plain | image-mosaic~f5233d054/arctic-cyan/outline/shaped | product-demo~f51bd2714/arctic-cyan/stacked/cutout | data-calculator/arctic-cyan/caps/plain | catalogue-gallery~f51dad164/noir-lime/stacked/textwrap | statement-display~f519f76b4/arctic-cyan/stacked/plain
  info  software seed 97: statement-panel~f305bb0a4/noir-lime/stacked/plain | image-mosaic~f30e620c4/arctic-cyan/outline/shaped | product-demo/noir-lime/stacked/cutout | catalogue-rail/arctic-cyan/stacked/textwrap | data-calculator/arctic-cyan/caps/plain | statement-display~f3051c724/arctic-cyan/stacked/plain
  ok    festival, ceramics-shop and software produce different COMPLETE sets across seeds
  info  LIVE festival (live jev-1.13.0, 359ms) seed 11: compositions 6/4, constructions 4/3, treatments 4/3, grayscale 6/4 — met=true
  info  LIVE festival (live jev-1.13.0, 359ms) seed 42: compositions 6/4, constructions 4/3, treatments 4/3, grayscale 6/4 — met=true
  info  LIVE festival (live jev-1.13.0, 359ms) seed 97: compositions 6/4, constructions 4/3, treatments 4/3, grayscale 6/4 — met=true
  info  LIVE shop (live jev-1.13.0, 229ms) seed 11: compositions 6/4, constructions 5/3, treatments 4/3, grayscale 6/4 — met=true
  info  LIVE shop (live jev-1.13.0, 229ms) seed 42: compositions 6/4, constructions 5/3, treatments 4/3, grayscale 6/4 — met=true
  info  LIVE shop (live jev-1.13.0, 229ms) seed 97: compositions 6/4, constructions 5/3, treatments 4/3, grayscale 6/4 — met=true
  info  LIVE software (live jev-1.13.0, 283ms) seed 11: compositions 6/4, constructions 4/3, treatments 4/3, grayscale 6/4 — met=true
  info  LIVE software (live jev-1.13.0, 283ms) seed 42: compositions 6/4, constructions 4/3, treatments 4/3, grayscale 6/4 — met=true
  info  LIVE software (live jev-1.13.0, 283ms) seed 97: compositions 6/4, constructions 4/3, treatments 4/3, grayscale 6/4 — met=true
  ok    a LIVE Jev decision also yields sets that meet every diversity target

all 22 checks passed


=== TurboSlop — locks, regeneration, batch versions ===

  ok    a lock binds to the card it came from, not to whatever is selected
  ok    unknown lock names are rejected with the list of what CAN be locked
  ok    a lock with no source direction is refused, not guessed
  ok    blueprint and composition locks are different, and incompatible ones explain why
  ok    a blueprint lock is actually applied during constrained regeneration
  ok    a composition lock pins the EXACT layout — base plus variant
  ok    styling locks from one card survive regeneration with that card’s exact values
  ok    an impossible lock combination fails with an explanation, not an empty set
  ok    regeneration with an unknown lock name fails loudly through the same path the API uses
  ok    the previous batch is preserved immutably — previews, metadata and links
  ok    the archived batch is reloadable from disk after further regeneration
  ok    regeneration is deterministic: same session inputs + same seed = same set
  ok    direction ids are stable identities derived from their own inputs
  ok    the session survives a save/load round trip with its versions intact

all 14 checks passed


=== TurboSlop — scoped revision ===

  ok    scope classification defaults to COPY: a wording request can never reach a visual axis
  ok    an offline copy-only revision changes NO visual property
  ok    revision never re-decides: the stored decision is byte-identical afterwards
  ok    the finalized parent is untouched by the revision
  ok    revision requires a finalized design first
  ok    a visual revision touches only the axes the request names
  ok    a visual request that matches no catalog axis changes nothing and says why
  ok    the session records revisions without losing its batches or versions

all 8 checks passed


=== TurboSlop — shared asset plan (controlled fixture service) ===

  ok    an items:table layout renders no catalogue plates, so it owns no items slots
  ok    an empty gallery renders no figures, so gallery slots are not renderable
  ok    a layout with imageSlots: 0 has no slots at all
  ok    zero renderable slots make zero requests to the asset service
  ok    the CLI pipeline path turns an enabled image setting into an image-capable layout and real requests
  ok    supplied images suppress generation for their slots; the rest are filled
  ok    when every renderable slot is supplied, generation is skipped entirely
  ok    a supplied image for a slot this layout does not render is REFUSED with a note
  ok    every generated asset renders in its own slot, verified by data-slot
  ok    slot-less legacy assets are reported as unverifiable rather than silently passing
  ok    finalizing the selected direction honours the image setting and verifies placement
  ok    the ZIP export carries assets, fonts WITH licences, and a self-contained page
  ok    content diagnostics are reported for what the inventory cannot fill

all 13 checks passed


=== TurboSlop — preview viewports (real control surface) ===

  ok    desktop mode simulates a real 1440×900 viewport inside the card
  ok    the page inside the desktop iframe lays out at 1440 CSS pixels
  ok    mobile mode switches to a real 390×844 viewport
  ok    resizing the window changes only the scale — the simulated viewport is stable
  ok    preview labels live outside the generated page canvas
  ok    a preview reports ready only after fonts load, with motion frozen

all 6 checks passed

